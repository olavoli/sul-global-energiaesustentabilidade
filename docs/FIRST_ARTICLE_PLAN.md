# Plano do primeiro artigo

O piloto `rascunho-como-funciona-matriz-eletrica-brasileira` permanece um scaffold vazio, real (`isDemo: false`), em `draft` e invisível. Ele não contém resposta factual, números, datas, instituições, fontes ou imagem de terceiros.

## Estado atual

- autoria: marcador `autoria-pendente`, não verificado;
- estágio do draft: `scaffold`;
- pesquisa: `not-started`;
- fontes candidatas e confirmadas: zero;
- fact-checks: zero;
- imagem: pendente;
- distribuição: bloqueada.

## Sequência autorizada

```text
onboarding autorizado → autor verified
scaffold → pesquisa ready-for-writing
draft factual em writing → review-ready
review → approved → publish-check
```

Antes de escrever, o fundador deve fornecer ou apurar material real, validar o autor e completar o briefing. A redação factual começa somente quando `research:validate` passar. A promoção para review continua bloqueada até o texto, as fontes, os fact-checks, a imagem e os conflitos estarem revisados.

Nenhum comando publica automaticamente. O lançamento depende ainda de aprovação humana, ambiente oficial e URL pública configurada.
